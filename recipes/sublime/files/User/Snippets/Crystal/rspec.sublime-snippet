<snippet>
	<content><![CDATA[
describe ${1:Class} do
  subject(:${2:tool}) { described_class.new }

  describe "#${3:run}" do
    subject { ${2:tool}.${3:run} }

    it do
      expect(subject).to eq(${4:[]})
    end
  end
end

]]></content>
	<tabTrigger>qspec</tabTrigger>
	<scope>source.crystal</scope>
</snippet>
